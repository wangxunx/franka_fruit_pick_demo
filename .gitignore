# --- Generated artifacts (eval results, videos, scripted-demo frames) ---
outputs/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.ipynb_checkpoints/

# --- Tooling / OS / editor ---
.DS_Store
.vscode/
.idea/
MUJOCO_LOG.TXT

# Stray debug renders occasionally written next to the code.
franka_fruit_pick/**/*.png

# NOTE: assets/ and datasets/ are intentionally NOT ignored -- they are bundled so the
# demo runs out of the box. They hold large binaries; if you push them to GitHub, use
# git-lfs (see README) or host them separately and document the download step.
